- provide(:title, "Login")

%h2 Login
= form_for(:session, url: sessions_path) do |f|
  %p
    = f.label :nickname
    = f.text_field :nickname, class: "input"
  %p
    = f.label :password
    = f.password_field :password, class: "input"
  %p
    = f.submit value: "Login", class: "input"
